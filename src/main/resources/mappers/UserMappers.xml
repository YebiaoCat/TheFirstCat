<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.yb.democat.mapper.UserMapper">

<!--    <resultMap id="UserResultMap" type="cn.yb.democat.entity.UserEntity">-->
<!--        <result column="id" jdbcType="INTEGER" property="id" />-->
<!--        <result column="username" jdbcType="VARCHAR" property="userName" />-->
<!--        <result column="netname" jdbcType="VARCHAR" property="netName" />-->
<!--        <result column="userage" jdbcType="VARCHAR" property="userAge" />-->
<!--        <result column="usersex" jdbcType="VARCHAR" property="userSex" />-->
<!--        <result column="userdescribe" jdbcType="VARCHAR" property="userDescribe" />-->
<!--    </resultMap>-->

    <select id="AllUser" resultType="cn.yb.democat.entity.UserEntity">
        select id,username,password,netname,userage,
               (CASE WHEN usersex = 1 THEN '男'
                    WHEN usersex = 2 THEN '女'
                    ELSE '未知' END) AS usersex,
               userdescribe,userphone,emailaddress
             from user where username = #{username}
    </select>

    <select id="GetUserByUsernameAndPassword" resultType="cn.yb.democat.entity.UserEntity">
        select id,username,password,netname,userage,
               (CASE WHEN usersex = 1 THEN '男'
                     WHEN usersex = 2 THEN '女'
                     ELSE '未知' END) AS usersex,
               userdescribe,userphone,emailaddress
                from user where username = #{username} and password = #{password}
    </select>

    <update id="UpdateUserPassword" parameterType="cn.yb.democat.entity.UserEntity">
        update user set password = #{newPassword} WHERE id = #{id}
    </update>

    <insert id="InsertNewUser" parameterType="UserEntity" useGeneratedKeys="true" keyProperty="id">
        insert into user (username, password, userphone, emailaddress) values
                         (#{user.userName,jdbcType=VARCHAR}, #{user.passWord,jdbcType=VARCHAR},#{user.userPhone,jdbcType=VARCHAR},#{user.emailAddress,jdbcType=VARCHAR})
    </insert>

    <select id="GetUserById" resultType="UserEntity">
        select id,username,password,netname,userage,
               (CASE WHEN usersex = 1 THEN '男'
                     WHEN usersex = 2 THEN '女'
                     ELSE '未知' END) AS usersex,
               userdescribe,userphone,emailaddress
        from user where id = #{id}
    </select>
</mapper>
